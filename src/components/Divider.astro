<hr />

<style lang="scss">
  @use "sass:map";
  @use "../styles/theme.scss";

  body[data-theme="dark"] {
    hr {
      border: 1px solid map.get(theme.$theme-dark, color-border);
    }
  }

  body[data-theme="light"] {
    hr {
      border: 1px solid map.get(theme.$theme-light, color-border);
    }
  }
</style>
